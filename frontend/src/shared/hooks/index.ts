/**
 * Shared Hooks - модуль экспорта всех хуков
 */

export { useWebSocket } from './useWebSocket';
export { useAuth } from './useAuth';
export { useRole, withRole, useAdminRole, useModeratorRole } from './useRole.js';
